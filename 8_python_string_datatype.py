# string is a text type data. if we want to store string in the variable we need to use '' or "" for data.
# if we take data from user end, python is taking as default string.

name = "kaka";
isCool = "Cool guy";

print(name + " is " + isCool);